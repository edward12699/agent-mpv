[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "py-agent"
version = "0.1.0"
description = "Python contract analysis agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["app*"]
