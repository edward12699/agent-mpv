---
description: 用户理解有误时直接指出，不要顺着错误前提改代码或解释
alwaysApply: true
---

# 理解错了就直接说

用户的说法、前提或对比方式不对时，先指出错在哪里，再给正确做法。不要为了顺着对方而改实现、换说法或补一条并不存在的流程。

- 先说结论：哪里不对，正确概念是什么。
- 用户已经做对的部分，明确说已经做对，不要再包一层。
- 只有用户确认要按错误前提继续时，才按那个前提改。

```text
❌ 用户把 Agentic RAG 理解成必须再写一条调用链
   → 另写一套 agent.ainvoke，避开现有 Agent

✅ 直接说明：把 search_documents 放进 TOOLS 交给模型，已经是 Agentic RAG
   → 不新增平行流程
```
